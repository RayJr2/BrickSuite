diff --git a/src/ui/builds/AllocateBuildRequirementDialog.cpp b/src/ui/builds/AllocateBuildRequirementDialog.cpp
--- a/src/ui/builds/AllocateBuildRequirementDialog.cpp
+++ b/src/ui/builds/AllocateBuildRequirementDialog.cpp
@@
-    m_table->setHorizontalHeaderLabels(QStringList() << "Storage Location"
-                                                     << "Owned"
-                                                     << "Other Builds"
-                                                     << "This Build"
-                                                     << "Available"
-                                                     << "Allocate");
+    m_table->setHorizontalHeaderLabels(QStringList() << "Storage Location"
+                                                     << "Owned"
+                                                     << "Other Allocations"
+                                                     << "This Requirement"
+                                                     << "Available"
+                                                     << "Allocate");
@@
-    m_partId = requirement->partId();
-
-    m_colorId = requirement->colorId();
+    //
+    // Build-from-Stock allocation follows the effective identity.
+    // The original requirement remains preserved on BuildRequirement.
+    //
+    m_partId = requirement->effectivePartId();
+
+    m_colorId = requirement->effectiveColorId();
@@
 void AllocateBuildRequirementDialog::loadInventory()
 {
@@
     BuildAllocationRepository allocationRepository;

+    const QList<BuildAllocation> requirementAllocations =
+        allocationRepository.getByRequirement(m_requirementId);
+
     const QList<InventoryRecord> records = inventoryRepository.getByPartColor(m_workspaceId,
                                                                               m_partId,
                                                                               m_colorId);
@@
     for (const InventoryRecord& record : records) {
         const int totalAllocated = allocationRepository.totalAllocatedForInventoryRecord(
             record.id());

-        const int thisBuildAllocated
-            = allocationRepository.totalAllocatedForInventoryRecordForBuild(record.id(), m_buildId);
+        int thisRequirementAllocated = 0;
+
+        for (const BuildAllocation& allocation : requirementAllocations) {
+            if (allocation.inventoryRecordId() == record.id()) {
+                thisRequirementAllocated += allocation.quantityAllocated();
+            }
+        }

-        const int otherBuildsAllocated = qMax(totalAllocated - thisBuildAllocated, 0);
+        const int otherAllocations = qMax(totalAllocated - thisRequirementAllocated, 0);

         //
-        // When editing an existing allocation,
-        // this Build's own allocation is available
-        // to it again.
+        // When editing an existing allocation for this exact requirement,
+        // that requirement's current reservation is available to it again.
         //
-        const int maximumForThisBuild = qMax(record.quantity() - otherBuildsAllocated, 0);
+        const int maximumForThisRequirement = qMax(record.quantity() - otherAllocations, 0);

         const int currentlyUnallocated = qMax(record.quantity() - totalAllocated, 0);
@@
-        auto* otherBuildsItem = new QTableWidgetItem(QString::number(otherBuildsAllocated));
+        auto* otherBuildsItem = new QTableWidgetItem(QString::number(otherAllocations));

-        auto* thisBuildItem = new QTableWidgetItem(QString::number(thisBuildAllocated));
+        auto* thisBuildItem = new QTableWidgetItem(QString::number(thisRequirementAllocated));
@@
-        allocationSpin->setRange(0, maximumForThisBuild);
+        allocationSpin->setRange(0, maximumForThisRequirement);

-        allocationSpin->setValue(thisBuildAllocated);
+        allocationSpin->setValue(thisRequirementAllocated);
@@
-        row.otherBuildsAllocated = otherBuildsAllocated;
+        row.otherBuildsAllocated = otherAllocations;

-        row.currentBuildAllocated = thisBuildAllocated;
+        row.currentBuildAllocated = thisRequirementAllocated;
@@
 void AllocateBuildRequirementDialog::saveAllocations()
 {
@@
-        //
-        // Find this Build's existing allocation
-        // against this exact inventory record.
-        //
+        //
+        // Find this requirement's existing allocation against this exact
+        // inventory record. Do not infer ownership from Build + Part/Color.
+        //
         const QList<BuildAllocation> allocations = allocationRepository.getByInventoryRecord(
             row.inventoryRecordId);

         std::optional<BuildAllocation> existingAllocation;

         for (const BuildAllocation& allocation : allocations) {
-            if (allocation.buildId() == m_buildId) {
+            if (allocation.buildId() == m_buildId
+                && allocation.buildRequirementId() == m_requirementId) {
                 existingAllocation = allocation;

                 break;
@@
         BuildAllocation allocation;

         allocation.setBuildId(m_buildId);
+        allocation.setBuildRequirementId(m_requirementId);

         allocation.setInventoryRecordId(row.inventoryRecordId);
diff --git a/src/services/builds/MissingPartsService.cpp b/src/services/builds/MissingPartsService.cpp
--- a/src/services/builds/MissingPartsService.cpp
+++ b/src/services/builds/MissingPartsService.cpp
@@
     for (const BuildRequirement& requirement :
          requirements)
     {
@@
         if (remaining <= 0)
             continue;

+        const int effectivePartId =
+            requirement.effectivePartId();
+
+        const int effectiveColorId =
+            requirement.effectiveColorId();
+
         const int owned =
             inventoryRepository
                 .totalQuantityForPartColor(
                     workspaceId,
-                    requirement.partId(),
-                    requirement.colorId());
+                    effectivePartId,
+                    effectiveColorId);

         const int totalAllocated =
             allocationRepository
                 .totalAllocatedForPartColor(
                     workspaceId,
-                    requirement.partId(),
-                    requirement.colorId());
+                    effectivePartId,
+                    effectiveColorId);

-        const int thisBuildAllocated =
-            allocationRepository
-                .totalAllocatedForPartColorForBuild(
-                    buildId,
-                    requirement.partId(),
-                    requirement.colorId());
+        int thisBuildAllocated =
+            allocationRepository
+                .totalAllocatedForRequirement(
+                    requirement.id());
+
+        //
+        // Schema-v21 allocations migrated to v22 intentionally have no
+        // requirement link. Preserve their behavior without attempting to
+        // guess a build_requirement_id.
+        //
+        if (thisBuildAllocated <= 0) {
+            thisBuildAllocated =
+                allocationRepository
+                    .totalAllocatedForPartColorForBuild(
+                        buildId,
+                        effectivePartId,
+                        effectiveColorId);
+        }
@@
         const std::optional<Part> part =
             partRepository.getById(
-                requirement.partId());
+                effectivePartId);

         const std::optional<Color> color =
             colorRepository.getById(
-                requirement.colorId());
+                effectiveColorId);
@@
         item.partId =
-            requirement.partId();
+            effectivePartId;

         item.colorId =
-            requirement.colorId();
+            effectiveColorId;
@@
                 ? part->partNumber()
                 : QString::number(
-                      requirement.partId());
+                      effectivePartId);
@@
                 ? color->name()
                 : QString::number(
-                      requirement.colorId());
+                      effectiveColorId);
diff --git a/src/ui/builds/ImportPullListDialog.cpp b/src/ui/builds/ImportPullListDialog.cpp
--- a/src/ui/builds/ImportPullListDialog.cpp
+++ b/src/ui/builds/ImportPullListDialog.cpp
@@
-        //
-        // Load the Build Requirement before making
-        // the fulfillment update.
-        //
-        const std::optional<BuildRequirement> requirement
-            = requirementRepository.getByBuildPartColor(m_buildId, row.partId, row.colorId, false);
+        //
+        // New schema-v22 allocations identify the exact Build Requirement.
+        // This is required for substituted requirements because allocation
+        // Part/Color is the effective identity while BuildRequirement retains
+        // the original source identity.
+        //
+        std::optional<BuildRequirement> requirement;
+
+        if (allocation->buildRequirementId() > 0) {
+            requirement =
+                requirementRepository.getById(allocation->buildRequirementId());
+
+            if (requirement
+                && (requirement->buildId() != m_buildId
+                    || requirement->effectivePartId() != row.partId
+                    || requirement->effectiveColorId() != row.colorId
+                    || requirement->isSpare())) {
+                requirement.reset();
+            }
+        } else {
+            //
+            // Legacy schema-v21 allocation. These rows were intentionally
+            // migrated with NULL build_requirement_id, so retain the old
+            // Part/Color lookup only for this compatibility case.
+            //
+            requirement =
+                requirementRepository.getByBuildPartColor(m_buildId,
+                                                           row.partId,
+                                                           row.colorId,
+                                                           false);
+        }

         if (!requirement) {
             database.rollback();

             QMessageBox::critical(this,
                                   "Reconcile Pull List",
-                                  QString("Unable to locate the Build Requirement "
-                                          "for part %1.\n\n"
+                                  QString("Unable to locate the exact Build Requirement "
+                                          "for part %1.\n\n"
                                           "No changes were saved.")
                                       .arg(row.partNumber));
diff --git a/src/ui/builds/BuildsWidget.cpp b/src/ui/builds/BuildsWidget.cpp
--- a/src/ui/builds/BuildsWidget.cpp
+++ b/src/ui/builds/BuildsWidget.cpp
@@
-    for (int column = 3; column <= 13; ++column) {
+    for (int column = 3; column <= 12; ++column) {
         m_requirementsTable->horizontalHeader()->setSectionResizeMode(column,
                                                                       QHeaderView::ResizeToContents);
     }
@@
     int row = 0;

     for (const BuildRequirement& requirement : requirements) {
-        const std::optional<Part> part = partRepository.getById(requirement.partId());
+        const int effectivePartId = completeSet
+                                        ? requirement.partId()
+                                        : requirement.effectivePartId();

-        const std::optional<Color> color = colorRepository.getById(requirement.colorId());
+        const int effectiveColorId = completeSet
+                                         ? requirement.colorId()
+                                         : requirement.effectiveColorId();
+
+        const std::optional<Part> part = partRepository.getById(effectivePartId);
+
+        const std::optional<Color> color = colorRepository.getById(effectiveColorId);
@@
         auto* colorItem = new QTableWidgetItem(colorName);
+
+        if (!completeSet
+            && (effectivePartId != requirement.partId()
+                || effectiveColorId != requirement.colorId())) {
+            const std::optional<Part> originalPart =
+                partRepository.getById(requirement.partId());
+
+            const std::optional<Color> originalColor =
+                colorRepository.getById(requirement.colorId());
+
+            const QString originalPartText =
+                originalPart ? originalPart->partNumber()
+                             : QString::number(requirement.partId());
+
+            const QString originalColorText =
+                originalColor ? originalColor->name()
+                              : QString::number(requirement.colorId());
+
+            const QString substitutionToolTip =
+                QString("Substitution for original requirement: %1 / %2")
+                    .arg(originalPartText, originalColorText);
+
+            partNumberItem->setToolTip(substitutionToolTip);
+            nameItem->setToolTip(substitutionToolTip);
+            colorItem->setToolTip(substitutionToolTip);
+        }
@@
         const int owned = inventoryRepository.totalQuantityForPartColor(workspaceId,
-                                                                        requirement.partId(),
-                                                                        requirement.colorId());
+                                                                        effectivePartId,
+                                                                        effectiveColorId);
@@
         const int totalAllocated = allocationRepository
                                        .totalAllocatedForPartColor(workspaceId,
-                                                                   requirement.partId(),
-                                                                   requirement.colorId());
+                                                                   effectivePartId,
+                                                                   effectiveColorId);
@@
-        const int thisBuildAllocated
-            = allocationRepository.totalAllocatedForPartColorForBuild(m_selectedBuildId,
-                                                                      requirement.partId(),
-                                                                      requirement.colorId());
+        int thisBuildAllocated =
+            allocationRepository.totalAllocatedForRequirement(requirement.id());
+
+        //
+        // Legacy schema-v21 allocations have no requirement link. Preserve
+        // their existing totals without attempting an ambiguous backfill.
+        //
+        if (thisBuildAllocated <= 0) {
+            thisBuildAllocated =
+                allocationRepository.totalAllocatedForPartColorForBuild(m_selectedBuildId,
+                                                                        effectivePartId,
+                                                                        effectiveColorId);
+        }
@@
     for (const BuildRequirement& requirement : requirements) {
@@
         if (remainingRequired <= 0)
             continue;

-        int thisBuildAllocated = allocationRepository.totalAllocatedForPartColorForBuild(
-            m_selectedBuildId,
-            requirement.partId(),
-            requirement.colorId());
+        const int effectivePartId = requirement.effectivePartId();
+        const int effectiveColorId = requirement.effectiveColorId();
+
+        int thisBuildAllocated =
+            allocationRepository.totalAllocatedForRequirement(requirement.id());
+
+        if (thisBuildAllocated <= 0) {
+            thisBuildAllocated =
+                allocationRepository.totalAllocatedForPartColorForBuild(m_selectedBuildId,
+                                                                        effectivePartId,
+                                                                        effectiveColorId);
+        }
@@
         const QList<InventoryRecord> sourceRecords =
             inventoryRepository.getByPartColor(
                 m_workspaceContext.currentWorkspaceId(),
-                requirement.partId(),
-                requirement.colorId());
+                effectivePartId,
+                effectiveColorId);
@@
         for (const InventoryRecord& record : records) {
@@
             const int totalAllocated = allocationRepository.totalAllocatedForInventoryRecord(
                 record.id());

-            const int currentBuildAllocated
-                = allocationRepository.totalAllocatedForInventoryRecordForBuild(record.id(),
-                                                                                 m_selectedBuildId);
+            const QList<BuildAllocation> recordAllocations =
+                allocationRepository.getByInventoryRecord(record.id());

-            const int otherBuildsAllocated = qMax(totalAllocated - currentBuildAllocated, 0);
+            int currentRequirementAllocated = 0;

-            const int maximumForThisBuild = qMax(record.quantity() - otherBuildsAllocated, 0);
+            std::optional<BuildAllocation> existingAllocation;

-            const int additionalCapacity = qMax(maximumForThisBuild - currentBuildAllocated, 0);
+            for (const BuildAllocation& allocation : recordAllocations) {
+                if (allocation.buildId() == m_selectedBuildId
+                    && allocation.buildRequirementId() == requirement.id()) {
+                    existingAllocation = allocation;
+                    currentRequirementAllocated += allocation.quantityAllocated();
+                }
+            }
+
+            const int otherAllocations =
+                qMax(totalAllocated - currentRequirementAllocated, 0);
+
+            const int maximumForThisRequirement =
+                qMax(record.quantity() - otherAllocations, 0);
+
+            const int additionalCapacity =
+                qMax(maximumForThisRequirement - currentRequirementAllocated, 0);
@@
-            const int newAllocationQuantity = currentBuildAllocated + quantityToAdd;
-
-            const QList<BuildAllocation> recordAllocations = allocationRepository.getByInventoryRecord(
-                record.id());
-
-            std::optional<BuildAllocation> existingAllocation;
-
-            for (const BuildAllocation& allocation : recordAllocations) {
-                if (allocation.buildId() == m_selectedBuildId) {
-                    existingAllocation = allocation;
-                    break;
-                }
-            }
+            const int newAllocationQuantity =
+                currentRequirementAllocated + quantityToAdd;
@@
                 BuildAllocation allocation;

                 allocation.setBuildId(m_selectedBuildId);
+                allocation.setBuildRequirementId(requirement.id());
                 allocation.setInventoryRecordId(record.id());
-                allocation.setPartId(requirement.partId());
-                allocation.setColorId(requirement.colorId());
+                allocation.setPartId(effectivePartId);
+                allocation.setColorId(effectiveColorId);
                 allocation.setStorageLocationId(record.storageLocationId());
                 allocation.setQuantityAllocated(quantityToAdd);
@@
                     qCritical() << "Allocate Available failed creating allocation."
                                 << "BuildId:" << m_selectedBuildId
+                                << "RequirementId:" << requirement.id()
                                 << "InventoryRecordId:" << record.id()
-                                << "PartId:" << requirement.partId()
-                                << "ColorId:" << requirement.colorId()
+                                << "PartId:" << effectivePartId
+                                << "ColorId:" << effectiveColorId
                                 << "Quantity:" << quantityToAdd;
@@
-        const int allocated = allocationRepository.totalAllocatedForPartColorForBuild(
-            m_selectedBuildId,
-            requirement.partId(),
-            requirement.colorId());
+        int allocated =
+            allocationRepository.totalAllocatedForRequirement(requirement.id());
+
+        if (allocated <= 0) {
+            allocated =
+                allocationRepository.totalAllocatedForPartColorForBuild(
+                    m_selectedBuildId,
+                    requirement.effectivePartId(),
+                    requirement.effectiveColorId());
+        }
